export enum Gender {
  Male = "Male",
  Female = "Female",
}

export enum Role {
  User = "user",
  Admin = "admin",
}

export enum UserStatus {
  Pending = "pending",
  Active = "active",
  Deleted = "deleted",
  Banned = "banned",
}
